import { Button } from "@/components/ui/button";
const button = () => {
  return (
    <div className="mt-8">
      <Button variant="outline" className="w-full bg-greyDarker text-white">
        Submit
      </Button>
    </div>
  );
};

export default button;
